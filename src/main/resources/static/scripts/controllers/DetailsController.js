/**
 * Created by daniel.baker on 3/1/2015.
 */
(function(){
    var app = angular.module("moneybags");
    var detailsController = function($scope, $http, details) {
        // set scope variables and functions
    };
    app.controller('DetailsController', detailsController);
}());